---
description: "[legado] Usar regra global token-efficiency.mdc"
alwaysApply: false
---

Context-mode: `~/.cursor/rules/token-efficiency.mdc`. Comandos repo (`pytest`, `git diff`, logs Docker, etc.) → `ctx_*` MCP conforme regra global.
